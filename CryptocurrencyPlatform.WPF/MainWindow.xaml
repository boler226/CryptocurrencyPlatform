<Window x:Class="CryptocurrencyPlatform.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CryptocurrencyPlatform.WPF.ViewModels"
        xmlns:views="clr-namespace:CryptocurrencyPlatform.WPF.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Top" Orientation="Horizontal" Background="LightGray" Height="40">
            <Button Content="Home" Command="{Binding ShowHomeCommand}" Margin="5"/>
        </StackPanel>
        
        <ContentControl Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
