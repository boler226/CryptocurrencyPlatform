<Window x:Class="CryptocurrencyPlatform.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CryptocurrencyPlatform.WPF.ViewModels"
        xmlns:views="clr-namespace:CryptocurrencyPlatform.WPF.Views"
        mc:Ignorable="d"
        WindowState="Maximized">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AssetViewModel}">
            <views:HomeView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" VerticalAlignment="Top" Orientation="Horizontal" Background="LightGray" Height="40">
            <Button Content="Home" Command="{Binding ShowHomeCommand}" Margin="5"/>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
